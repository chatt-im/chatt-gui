[package]
name = "chatt-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
ash = "0.38"
async-channel = "2.5"
gpui = { path = "/code/ref/zed/crates/gpui", default-features = false, features = ["wayland"] }
gpui_platform = { path = "/code/ref/zed/crates/gpui_platform", default-features = false, features = ["wayland"] }
gpui_wgpu = { path = "/code/ref/zed/crates/gpui_wgpu", default-features = false }
image = { version = "0.25", default-features = false }
libc = "0.2"
libmpv2 = { path = "vendor/libmpv2-rs", default-features = false, features = ["render", "vendored"] }
log = "0.4"
markdown = { path = "/code/ref/zed/crates/markdown", default-features = false }
rpc = { package = "chatt-rpc", path = "crates/chatt_rpc" }
theme = { path = "/code/ref/zed/crates/theme" }
unicode-segmentation = "1"
aws-lc-rs = { version = "1.17", default-features = false, features = ["alloc", "non-fips"] }
tempfile = "3"

[profile.dist]
inherits = "release"
lto = "fat"
codegen-units = 1

[profile.dev]
debug = 0

[profile.release]
debug = 1

[patch.crates-io]
naga = { path = "vendor/wgpu/naga" }
wgpu = { path = "vendor/wgpu/wgpu" }
wgpu-core = { path = "vendor/wgpu/wgpu-core" }
wgpu-hal = { path = "vendor/wgpu/wgpu-hal" }
wgpu-naga-bridge = { path = "vendor/wgpu/wgpu-naga-bridge" }
wgpu-types = { path = "vendor/wgpu/wgpu-types" }
